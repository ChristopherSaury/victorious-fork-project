{% extends 'base-admin.html.twig' %}

{% block title %}Ajouter un plat{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-lg d-flex align-items-center flex-column">
            <h2 class="text-center m-3">Ajouter un plat</h2>
            <div class="form-container" style="width: 50%;">
                {{ form(dishForm) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}